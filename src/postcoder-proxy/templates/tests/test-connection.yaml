apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postcoder-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "postcoder-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postcoder-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
